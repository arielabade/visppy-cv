# VISUAL SLOT — Benchmark chart

Purpose: quantify the trade-off between accuracy, latency and compute.

Suggested asset: benchmark chart with model/runtime/device, FPS, end-to-end latency, confidence or accuracy metric and test-scene description.

Preferred format: SVG or PNG plus a Markdown table with the underlying values.

Suggested aspect ratio: 16:9.


Required annotation: dataset or scene scope, sample size and whether values are measured or illustrative.