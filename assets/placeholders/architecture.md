# VISUAL SLOT — Architecture diagram

Purpose: replace or complement the Mermaid diagram with an approved implementation diagram when upstream infrastructure evidence is available.

Suggested asset: camera/stream ingress → inference → tracking → storage/API → dashboard, with a clear edge/cloud boundary.

Preferred format: SVG or Mermaid source.

Suggested aspect ratio: wide landscape.


Recommended location: `docs/architecture.md`.